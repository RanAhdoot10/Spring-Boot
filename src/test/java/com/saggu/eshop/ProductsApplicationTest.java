package com.saggu.eshop;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ProductsApplicationTest {

    // Check whether or not I'm able to start my spring boot application
    @Test
    public void loadContext() {
        // no Impl needed as we are simply loading the context
    }
}
